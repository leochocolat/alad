class ThreeImagesGrid {
    constructor(options) {
        const { width, height, scene } = options;

        this.width = width;
        this.height = height;
        this.scene = scene;

        this._setup();
    }

    //public
    update() {

    }

    resize() {
        
    }

    //private
    _setup() {
        
    }
}

export default ThreeImagesGrid;